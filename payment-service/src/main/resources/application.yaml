spring:
    application:
      name: payment-service

server:
  port: 8082

grpc:
  client:
    user-service:
      address: static://localhost:9090
      negotiationType: plaintext

    wallet-service:
      address: static://localhost:9091
      negotiationType: plaintext

jwt:
  secret: change-this-to-a-long-random-secret-value-at-least-32-bytes
